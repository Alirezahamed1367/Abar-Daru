# 🚀 راهنمای اجرای اولیه سیستم مدیریت انبار دارو

## ⚠️ اولین اجرا - مهم!

دیتابیس به صورت خالی ایجاد شده است و **هیچ کاربر پیش‌فرضی ندارد**.

---

## 📋 مراحل راه‌اندازی:

### **1️⃣ نصب وابستگی‌ها**

```bash
# Backend
cd backend
pip install -r requirements.txt

# Frontend
cd ../frontend
npm install
```

---

### **2️⃣ ساخت فایل‌های استاتیک فرانت‌اند**

```bash
cd frontend
npm run build
```

---

### **3️⃣ اجرای سرور**

```bash
# از پوشه اصلی پروژه
python run_server.py
```

سرور روی پورت 8000 اجرا می‌شود:
- 🌐 Local: http://localhost:8000
- 🌍 Network: http://YOUR_IP:8000

---

### **4️⃣ ایجاد اولین کاربر (مدیر کل)**

⚠️ **مهم:** از آنجایی که دیتابیس خالی است، باید اولین کاربر را **به صورت دستی** ایجاد کنید.

#### **روش 1: استفاده از API مستقیم (پیشنهادی)**

با یک ابزار مثل Postman یا curl:

```bash
curl -X POST "http://localhost:8000/api/users" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "YOUR_SECURE_PASSWORD",
    "full_name": "مدیر سیستم",
    "access_level": "superadmin"
  }'
```

#### **روش 2: از طریق کد Python**

یک فایل موقت بسازید (`create_first_user.py`):

```python
from backend.database import SessionLocal
from backend.models import User
from passlib.context import CryptContext

db = SessionLocal()
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

# ایجاد اولین کاربر
user = User(
    username="admin",
    password=pwd_context.hash("YOUR_SECURE_PASSWORD"),  # رمز دلخواه
    full_name="مدیر سیستم",
    access_level="superadmin"
)

db.add(user)
db.commit()
db.close()
print("✅ کاربر مدیر با موفقیت ایجاد شد!")
```

سپس اجرا کنید:
```bash
python create_first_user.py
```

---

### **5️⃣ ورود به سیستم**

1. مرورگر را باز کنید: http://localhost:8000
2. با نام کاربری و رمز عبوری که ساختید وارد شوید
3. از پنل مدیریت، سایر کاربران را ایجاد کنید

---

## 🔐 سطوح دسترسی:

- **superadmin**: مدیر کل (دسترسی به همه چیز)
- **admin**: مدیر (مدیریت کاربران و تنظیمات)
- **warehouseman**: انباردار (مدیریت موجودی انبار خاص)
- **viewer**: مشاهده‌گر (فقط مشاهده)

---

## 📁 ساختار پروژه:

```
Anbar-Daru/
├── backend/
│   ├── pharmacy.db          # دیتابیس SQLite (خالی)
│   ├── main.py              # سرور FastAPI
│   ├── models.py            # مدل‌های دیتابیس
│   └── requirements.txt     # وابستگی‌های Python
├── frontend/
│   ├── src/                 # کد React
│   └── build/               # فایل‌های استاتیک (بعد از build)
├── run_server.py            # اسکریپت اجرای سرور
└── FIRST_RUN.md            # این فایل
```

---

## 🛠️ عیب‌یابی:

### مشکل: "Directory build/static does not exist"
**راه حل:**
```bash
cd frontend
npm run build
```

### مشکل: "No module named ..."
**راه حل:**
```bash
cd backend
pip install -r requirements.txt
```

### مشکل: نمی‌توانم وارد شوم
**راه حل:** مطمئن شوید اولین کاربر را ایجاد کرده‌اید (مرحله 4)

---

## 📞 پشتیبانی:

برای هر مشکلی، به مستندات یا issues گیت‌هاب مراجعه کنید.

**موفق باشید! 🎉**
